import React from "react";

export const metadata = {
  title: "About Third Teen",
};

function layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

export default layout;
